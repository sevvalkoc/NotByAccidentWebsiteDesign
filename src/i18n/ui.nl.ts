/* PLACEHOLDER — pending Dutch translation. Mirrors src/i18n/ui.ts's `en`
   dictionary exactly (same shape) so the app builds while translation is
   in progress. Every value below must be replaced with real Dutch copy. */
import { en } from '@/i18n/ui'
import type { UiStrings } from '@/i18n/ui'

export const ui: UiStrings = en
