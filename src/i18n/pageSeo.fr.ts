/* PLACEHOLDER — pending French translation. Mirrors src/i18n/pageSeo.ts's
   `en` map exactly (same keys) so the app builds while translation is in
   progress. Every value below must be replaced with real French copy. */
import { en } from '@/i18n/pageSeo'
import type { PageSeoMap } from '@/i18n/pageSeo'

export const pageSeo: PageSeoMap = en
